# Frozen Splits

Store fixed split files here.

Recommended split set:

- `smoke.jsonl`
- `dev.jsonl`
- `holdout.jsonl`
- `audit.jsonl`

Once created, keep these files stable to avoid evaluation drift.

