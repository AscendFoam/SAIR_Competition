# Artifacts

This directory stores generated assets that should not live under source control forever, but whose structure matters.

- `candidates/`: high-value prompt candidates and evaluation summaries
- `final/`: frozen release assets
- `backups/`: manual backups

